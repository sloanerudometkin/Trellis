# Copy this file to .env.test only when PostgreSQL integration tests are added.
# The database name must contain "test", and this example contains no real secret.
TEST_DATABASE_URL=postgresql+psycopg://trellis_test_user:replace-me@127.0.0.1:5432/trellis_test
